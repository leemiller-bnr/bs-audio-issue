{
  "name": "bs-sound-issue",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && yarn install && yarn build && cd ../server && yarn install && yarn start",
    "build": "cd client && yarn install && yarn build && cd ../server && yarn install && npx webpack --mode production"
  }
}
